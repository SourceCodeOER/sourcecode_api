const path = require("path");

module.exports = {
    // location to store temp / definitive file
    "UPLOAD_FOLDER": path.resolve(__dirname, "..", "uploads/"),
    "FILES_FOLDER": path.resolve(__dirname, "..", "files/")
}